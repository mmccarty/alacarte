		<div id="update">   
		<p>	Last Update: <%=@page.updated_at.to_formatted_s(:short)%> <%if @page.tag_list.length > 0 %> | Tagged with: <% @page.tag_list.each do |tag|%> <%= link_to tag, { :action => :tagged, :id => tag }%><%end%> <%end%></p>
		</div>
			
