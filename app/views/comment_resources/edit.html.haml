%h1 Edit Module

= form_for @mod do |f|
  = error_messages_for :mod

  %label{ for: 'mod_label' }
    Private Label
    %span.required *
  = f.text_field :label, size: 55

  %label{ for: 'mod_title' }
    Module Title
    %span.required *
  = f.text_field :module_title, size: 55

  %label{ for: 'mod_tag_list' } Tags
  = f.text_field :tag_list, size: 55
  .tip example: WR101, Databases, Writing

  %label{ for: 'mod_topic' } Content
  = f.text_area :topic, class: 'tinymce'
  = tinymce
  %br/

  %label{ for: 'mod_num_displayed' } Number of Comments to Display
  = f.select :num_displayed, [5, 10, 20, 50, 100]
  %br/

  = f.submit class: 'btn btn-primary'
  = link_to 'Cancel', modules_path, class: 'btn btn-cancel'
