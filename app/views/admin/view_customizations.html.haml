%fieldset
  %h2 Customizations
  %h3 View
  %div
    = link_to "Course Guide Template" , :controller => 'ica', :action => 'published_pages'
    | #{link_to "Subject Guide Template" , :controller => 'srg', :action => 'published_guides'} |
    \#{link_to "Tutorial Template" , :controller => 'ort', :action => 'published_tutorials'}
  %h3 Edit
  %div
    = link_to "Template" , :controller => 'admin', :action => 'customize_layout'
    | #{link_to "Search" , :controller => 'admin', :action => 'customize_search'} |
    \#{link_to "Content Types" , :controller => 'admin', :action => 'customize_content_types'} |
    \#{link_to "Admin Email Addresses" , :controller => 'admin', :action => 'customize_admin_email'}
  %p
    - for column in Local.content_columns
      - unless @local.send(column.name).blank? || column.human_name == 'Types'
        %b
          = column.human_name
          \:
        = h @local.send(column.name)
        %br/
