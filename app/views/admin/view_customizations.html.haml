%h1 Customizations

%h2 View
%ul.unstyled
  %li= link_to "Course Guide Template" , :controller => 'ica', :action => 'published_pages'
  %li= link_to "Subject Guide Template" , :controller => 'srg', :action => 'published_guides'
  %li= link_to "Tutorial Template" , :controller => 'ort', :action => 'published_tutorials'

%h2 Edit
%ul.unstyled
  %li= link_to "Template" , :controller => 'admin', :action => 'customize_layout'
  %li= link_to "Content Types" , :controller => 'admin', :action => 'customize_content_types'
  %li= link_to "Admin Email Addresses" , :controller => 'admin', :action => 'customize_admin_email'

%ul.unstyled
  - Local.content_columns.each do |column|
    - unless @local.send(column.name).blank? || column.human_name == 'Types'
      %li <strong>#{ column.human_name }:</strong> #{ @local.send(column.name) }
