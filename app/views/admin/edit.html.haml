%fieldset
  %h3
    Edit Account for #{h @user.name}
  = form_tag :action => 'update', :id => @user do
    = render :partial => 'form'
    = submit_tag 'Edit Account', :class=>"submit"
  %p
    %b= link_to 'Cancel', :action => 'users'
