%h2= h @mod.module_title
.module
  - unless @mod.widget.blank?
    = @mod.widget
    %noscript
      You must have javascript enabled to view this content.
    %noembed
      You do allow embedded content.
  - unless @mod.information.blank?
    = @mod.information
