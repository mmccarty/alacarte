%h2= h @mod.module_title
.module
  - unless @mod.information.blank?
    = @mod.information
  - @mod.books.each do |book|
    #book_view
      = render :partial => '/book/view_book', :locals => {:book => book}
