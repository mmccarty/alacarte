.container
  #page-header.page-header
    %h1
      = header_title
    %p.small
      = yield :about

  .container
    .row
      = yield :left_sidebar

      .span8#skip
        = yield :tab_headings
        .tab-content
          = yield :tab_content

      = yield :right_sidebar
