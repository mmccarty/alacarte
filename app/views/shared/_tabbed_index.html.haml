.container
  .container#page-header{ :data => {:spy => 'affix', :offset => { :top => 150, :bottom => 150 }}}
    %h1.page-header
      = header_title

  .container
    .row
      = yield :left_sidebar

      .span8
        = yield :tab_headings
        .tab-content
          = yield :tab_content

      = yield :right_sidebar
