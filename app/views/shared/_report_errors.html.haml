- if object.errors.any?
  .alert.alert-error
    %p
      %i.icon-warning-sign
      Please correct the following errors and retry:
    %ul
      - object.errors.full_messages.each do |msg|
        %li
          = msg
