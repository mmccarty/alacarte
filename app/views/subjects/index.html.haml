%h1 Listing Subjects
= flash_notice

= link_to new_subject_path, class: 'btn btn-primary' do
  %i.icon-plus
    Create New Subject

%table.table.table-striped
  %thead
    %tr
      %th Subject Code
      %th Subject Name
      %th Actions
  %tbody
    - @subjects.each do |subject|
      %tr
        %td= subject.subject_code
        %td= subject.subject_name
        %td
          = link_to '', edit_subject_path(subject), class: 'icon-edit'
          = link_to '', subject_path(subject), class: 'icon-trash', :confirm => 'Are you sure you want to delete this subject?', :method => :delete

= will_paginate @subjects
