- if @page.present?
  = render 'page/side_bar'
  = render 'page/title_actions'
- elsif @guide.present?
  = render 'guide/side_bar'
  = render 'guide/title_actions'
- else
  = render 'side_bar'
  %span= @mod.module_title
  .actions
    = link_to image_tag("icons/document_preview.png", :title => 'Preview this module', :alt => 'preview', :border => 0), node_path(@mod), :target => '_blank'
    = link_to 'Preview', node_path(@mod), :target => '_blank'
