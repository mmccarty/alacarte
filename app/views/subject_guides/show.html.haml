- content_for :ada_navigation do
  = link_to 'Go to screenreader version' , { :action => 'mobile_index', :id => @guide } , :id => 'alt-version'

= render :partial => "shared/page_index", :locals => { :header_title => @guide.guide_name, :index => @guide, :index_controller => 'subject_guides', :index_description => @guide.description }
