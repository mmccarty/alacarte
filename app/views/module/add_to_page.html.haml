%fieldset
  %h3.title
    = render :partial => 'title_actions'
  %h4
    = link_to "Add To/Used On", :controller => 'module', :action =>'manage', :id => @mod.id, :type => @mod.class
    >> Add to Course Pages
  - if flash[:error]
    #error_wrapper
      #list_error= flash[:error]
  %p Check the box next to the tabs on the pages you want to add the module to then click the button at the bottom of the list.
  - unless @pages.size <1
    = form_tag :action => 'add_to_page', :id => @mod.id, :type => @mod.class do
      #product-list
        #table
          = render :partial => "add_page_list"
      %div= submit_tag 'Add Module', :class=> "submit"
  - else
    %p
      %b You do not have any Course Pages to add to.
