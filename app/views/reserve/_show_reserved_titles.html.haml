- if reserves and !reserves.empty?
  - reserves.each do |r|
    .reserves
      %span.item_title= r[:title]
      %span.call_num
        Author: #{r[:author]}
      %span.call_num
        Call #: #{r[:availability]}
